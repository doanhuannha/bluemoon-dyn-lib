import { IFieldItem, IViewItem, KeyPair } from './Defs';

export const DataPool = {
  allControls: {} as KeyPair,
  allPages: {} as KeyPair,
  allViews: {} as IViewItem,
  allFields: {} as IFieldItem,
  allLayouts: {} as KeyPair,
}
